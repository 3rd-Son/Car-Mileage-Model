from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A model to predict the mileage of cas using the available features of the car',
    author='vic3sax',
    license='MIT',
)
